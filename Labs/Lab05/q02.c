// in message queue once the message is read by a process, other processes can't read the message as it gets removed from the queue, whereas in shared memory, message remains there only.

// shared memory doesn't require to copy messages to queue, so it is faster also.

